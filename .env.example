NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Server-only elevated key for invite/campaign RPCs (BYPASSRLS by design).
# Prefer Settings → API Keys → Secret key (sb_secret_...).
# Legacy fallback: service_role JWT still works if set instead.
SUPABASE_SECRET_KEY=
# SUPABASE_SERVICE_ROLE_KEY=
